services:
  particle-hunter:
    build: .
    ports:
      - "8082:80"    # http://localhost:8082
    volumes:
      # Live-reload: changes to index.html reflect instantly without rebuilding
      - ./index.html:/usr/share/nginx/html/index.html
